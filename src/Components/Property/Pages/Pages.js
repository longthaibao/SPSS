import styles from "./Pages.module.css";

const Pages = () => {
  return (
    <input
      className={styles.chooseNumofPage}
      placeholder="Chọn trang in"
      type="text"
    />
  );
};

export default Pages;
